const platformLogout = () => { swal("Ops, você não tem permissão para isso, tente logar novamente!", "Clique em ok para sair", "error").then(() => { localStorage.removeItem(btoa('idUser')); localStorage.removeItem(btoa('login')); localStorage.removeItem(btoa('idDefault')); localStorage.removeItem(btoa('emailUser')); localStorage.removeItem(btoa('permission')); window.location.href = "/admin/logout"; }); }